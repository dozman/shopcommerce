<form  class="form-topup" action="{request}" method="{form_method}" id="{form_id}" name="{form_name}"  autocomplete="{form_autocomplete}">
	<div class="clearfix"></div>
	{inputs}
	<button type="submit" class="btn btn-primary mb-2">Top-up</button>
</form>